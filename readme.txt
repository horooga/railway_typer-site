Service's user-defined(PORT) and shared(MONGO_PUBLIC_URL) vars:

+---MongoDB------+    +--typer-app---+
|vars:           | +->|vars:         |
|MONGO_PUBLIC_URL|-+  |PORT:8000     |
|                |    |              |
+----------------+    +--------------+
